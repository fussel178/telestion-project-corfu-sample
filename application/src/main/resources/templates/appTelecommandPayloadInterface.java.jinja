///
/// Autogenerated via corfu message generator. Do NOT edit!
///
package {{ package }};

import com.fasterxml.jackson.annotation.JsonSubTypes;
import com.fasterxml.jackson.annotation.JsonTypeInfo;
import {{ app_telecommand_payload_binary_name }};

/**
 * Telecommand payload for the app telecommand "{{ raw_name }}".
 */
@JsonTypeInfo(use = JsonTypeInfo.Id.NAME, property = "type")
@JsonSubTypes({
{% for payload in payloads %}		@JsonSubTypes.Type(value = {{ payload.class_name }}.class, name = {{ payload.class_name }}.TELECOMMAND_PAYLOAD_NAME),
{% endfor -%}
})
public interface {{ uppercamelcase_name }}TelecommandPayload extends AppTelecommandPayload {
}
