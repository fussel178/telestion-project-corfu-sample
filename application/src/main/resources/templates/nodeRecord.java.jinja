///
/// Autogenerated via corfu message generator. Do NOT edit!
///
package {{ package }};

import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import {{ corfu_hardware_binary_name }};
import {{ corfu_node_binary_name }};

/**
 * Represents the node "{{ raw_name }}" from the Corfu configuration.
 * <p>
 * Description:
 * {{ description }}
 *
 * @param hardware the "{{ raw_name }}" hardware that sends or receives the message
 */
@JsonIgnoreProperties(ignoreUnknown = true)
public record {{ uppercamelcase_name }}Node({{ uppercamelcase_name }}Hardware hardware) implements CorfuNode {

	public static final String NODE_NAME = "{{ raw_name }}";
	public static final short NODE_ID = {{ id }};

	public enum {{ uppercamelcase_name }}Hardware implements CorfuHardware {
		{% for hardware in hardware_targets %}{{ hardware.name }}((short) {{ hardware.id }}){{ loop.revindex0 > 0 ? "," : ";" }}
		{% endfor %}
		private final short id;

		{{ uppercamelcase_name }}Hardware(short id) {
			this.id = id;
		}

		@Override
		public short id() {
			return id;
		}
	}

	@Override
	public short id() {
		return NODE_ID;
	}

	@Override
	public String name() {
		return NODE_NAME;
	}
}
