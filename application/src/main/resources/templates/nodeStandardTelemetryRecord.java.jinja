///
/// Autogenerated via corfu message generator. Do NOT edit!
///
package {{ package }};

import com.fasterxml.jackson.annotation.JsonProperty;
// import app standard telemetries
import {{ corfu_property_binary_name }};
{% for standard_telemetry in standard_telemetries -%}
import {{ standard_telemetry.binary_name }};
{% endfor -%}
// import app payload interface that sends the standard telemetry
import {{ standard_telemetry_app.payload_interface_binary_name }};
// internal imports
import {{ package }}.{{ uppercamelcase_name }}Node;

/**
 * Represents the standard telemetry of the node "{{ raw_name }}" from the Corfu configuration.
 */
public record {{ uppercamelcase_name }}StandardTelemetryPayload(
{% for standard_telemetry in standard_telemetries -%}
		@JsonProperty @CorfuProperty {{ standard_telemetry.class_name }} {{ standard_telemetry.name }}{{ loop.revindex0 > 0 ? "," : "" }}
{% endfor -%}
) implements {{ standard_telemetry_app.uppercamelcase_name }}TelemetryPayload {

	public static final String TELEMETRY_PAYLOAD_NAME = "{{ uppercamelcase_name }}StandardTelemetry";

	public static final short TELEMETRY_PAYLOAD_ID = {{ standard_telemetry_app.payload_id }};

	public static final short TELEMETRY_PAYLOAD_NODE_ID = {{ uppercamelcase_name }}Node.NODE_ID;

	@Override
	public short id() {
		return TELEMETRY_PAYLOAD_ID;
	}

	@Override
	public String name() {
		return TELEMETRY_PAYLOAD_NAME;
	}

	@Override
	public short nodeId() {
		return TELEMETRY_PAYLOAD_NODE_ID;
	}
}
