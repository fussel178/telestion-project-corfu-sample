///
/// Autogenerated via corfu message generator. Do NOT edit!
///
package {{ package }};

import com.fasterxml.jackson.annotation.JsonSubTypes;
import com.fasterxml.jackson.annotation.JsonTypeInfo;
import {{ app_telemetry_payload_binary_name }};

// standard telemetry imports
{% for node_payload in node_payloads %}import {{ node_payload.binary_name }};
{% endfor %}
// internal imports
{% for payload in payloads %}import {{ package }}.{{ payload.class_name }};
{% endfor %}
/**
 * Telemetry payload for the app telecommand "{{ raw_name }}".
 */
@JsonTypeInfo(use = JsonTypeInfo.Id.NAME, property = "type")
@JsonSubTypes({
{% for payload in (payloads + node_payloads) | list %}		@JsonSubTypes.Type(value = {{ payload.class_name }}.class, name = {{ payload.class_name }}.TELEMETRY_PAYLOAD_NAME),
{% endfor -%}
})
public interface {{ uppercamelcase_name }}TelemetryPayload extends AppTelemetryPayload {
}
